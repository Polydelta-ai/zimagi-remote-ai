plugin:
    remote_ai_model:
        base: data
        data: remote_ai_model
        providers:
            tdidf_svc:
                option:
                    predictor:
                        type: str
                        default: predictor
                        help: "Name of the prediction field within given dataset"
                    target:
                        type: str
                        default: target
                        help: "Name of the prediction target field within given dataset"
